# dependencies
import torch			# pytorch


def end2end(data: list[str]) -> torch.Tensor:
	'''

	'''

	return torch.zeros(3)


def fft(data: list[str]) -> torch.Tensor:
	'''

	'''

	return torch.zeros(3)


def mel(data: list[str]) -> torch.Tensor:
	'''

	'''

	return torch.zeros(3)


def q(data: list[str]) -> torch.Tensor:
	'''

	'''

	return torch.zeros(3)
